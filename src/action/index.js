import * as dashboard from 'action/dashboard';
import * as common from 'action/common';
import * as history from 'action/history';
import login from 'action/auth';

export default {
  login,
  dashboard,
  common,
  history
}